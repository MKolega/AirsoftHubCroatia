name: Deploy

on:
  push:
    branches: [ main ]

concurrency:
  group: deploy-main
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      VPS_SSH_KNOWN_HOSTS: ${{ secrets.VPS_SSH_KNOWN_HOSTS }}
    steps:
      - name: Checkout (for metadata)
        uses: actions/checkout@v4

      - name: Start SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}

      - name: Add VPS to known_hosts
        if: ${{ env.VPS_SSH_KNOWN_HOSTS != '' }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "${VPS_SSH_KNOWN_HOSTS}" >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts
        env:
          VPS_SSH_KNOWN_HOSTS: ${{ env.VPS_SSH_KNOWN_HOSTS }}

      - name: Add VPS to known_hosts (fallback)
        if: ${{ env.VPS_SSH_KNOWN_HOSTS == '' }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          ssh-keyscan -p "${VPS_SSH_PORT}" -H "${VPS_SSH_HOST}" >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts
        env:
          VPS_SSH_HOST: ${{ secrets.VPS_SSH_HOST }}
          VPS_SSH_PORT: ${{ secrets.VPS_SSH_PORT || '22' }}

      - name: Deploy
        run: |
          ssh -p "${VPS_SSH_PORT}" -o BatchMode=yes "${VPS_SSH_USER}@${VPS_SSH_HOST}" << EOF
          set -euo pipefail
          cd "${VPS_APP_DIR}"
          git fetch --all
          git reset --hard origin/main
          bash deploy/deploy.sh
          EOF
        env:
          VPS_SSH_HOST: ${{ secrets.VPS_SSH_HOST }}
          VPS_SSH_PORT: ${{ secrets.VPS_SSH_PORT || '22' }}
          VPS_SSH_USER: ${{ secrets.VPS_SSH_USER }}
          VPS_APP_DIR: ${{ secrets.VPS_APP_DIR }}
