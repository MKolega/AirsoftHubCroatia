{
	# Email is optional but helps with Let's Encrypt expiry notices.
	# email you@example.com
}

{$DOMAIN} {
	encode zstd gzip

	handle /api/* {
		reverse_proxy api:8080
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
